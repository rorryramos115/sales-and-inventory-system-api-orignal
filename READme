Browsers cache JavaScript files aggressively for performance.

Fixes:

Hard refresh:
On Windows: Ctrl + Shift + R
On Mac: Cmd + Shift + R

Disable cache in DevTools:

Open Chrome DevTools (F12).

Go to Network tab.

Tick Disable cache (while DevTools is open).

Add a version query to your script: